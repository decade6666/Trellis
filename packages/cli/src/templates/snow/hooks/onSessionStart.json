{
  "onSessionStart": [
    {
      "description": "Inject Trellis session context on Snow session start (additionalContext + .snow/log breadcrumb)",
      "hooks": [
        {
          "type": "command",
          "command": "python3 -X utf8 .snow/hooks/write-trellis-context.py session",
          "timeout": 20000,
          "enabled": true
        }
      ]
    }
  ]
}
