{
  "version": "0.6.11",
  "description": "Republish with a fixed core dependency publish path after the 0.6.10 packaging line.",
  "breaking": false,
  "recommendMigrate": false,
  "changelog": "**Release packaging:**\n- chore(cli): bump to 0.6.11 with a fixed publish path for the core dependency so the packed CLI resolves a registry core version correctly.\n\n**Continuity note:** the published tarball did not bundle a migration manifest. No filesystem migration was shipped in this package. The bundled `codeagent-wrapper` itself landed in the subsequent 0.6.12 publish.",
  "migrations": [],
  "notes": "Continuity placeholder: the published package did not bundle this manifest; no filesystem migration shipped in that tarball.\n\nNo `--migrate` required for this version bucket."
}
