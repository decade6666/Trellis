{
  "version": "0.6.16",
  "description": "Document the portable codeagent-wrapper path in the default bundled workflow and related install guidance.",
  "breaking": false,
  "recommendMigrate": false,
  "changelog": "**Enhancements:**\n- docs(workflow): ship portable `codeagent-wrapper` path guidance in the bundled native workflow so `trellis init` / `trellis update` install it by default.\n- docs: fix misleading `~/.claude/bin` examples in `config.yaml` and package README; bump marketplace native/tdd templates for the same guidance.\n\n**Continuity note:** the published tarball did not bundle a migration manifest. No filesystem migration was shipped in this package.",
  "migrations": [],
  "notes": "Continuity placeholder: the published package did not bundle this manifest; no filesystem migration shipped in that tarball.\n\nNo `--migrate` required for this version bucket. Run `trellis update` after upgrading to refresh workflow/docs templates."
}
