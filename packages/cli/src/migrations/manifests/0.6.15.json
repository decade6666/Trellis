{
  "version": "0.6.15",
  "description": "Point the default marketplace/template source at the decade6666 fork.",
  "breaking": false,
  "recommendMigrate": false,
  "changelog": "**Enhancements:**\n- chore(cli): switch default `TEMPLATE_INDEX_URL` / `TEMPLATE_REPO` from `mindfold-ai/marketplace` to `decade6666/marketplace` so `trellis update` / `init` / `workflow` resolve the fork by default.\n- chore(git): point the marketplace submodule URL at the fork.\n\n**Continuity note:** the published tarball did not bundle a migration manifest. No filesystem migration was shipped in this package.",
  "migrations": [],
  "notes": "Continuity placeholder: the published package did not bundle this manifest; no filesystem migration shipped in that tarball.\n\nNo `--migrate` required for this version bucket."
}
